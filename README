# U-Boot for RK3568 NAS Lite

这是面向 Rockchip RK3568 NAS Lite 目标设备的 U-Boot 源码，基于
`orangepi-xunlong/u-boot-orangepi`，加入了 NAS Lite 的默认配置和设备树。
当前默认板级源码与 2026-09-07 通过 Armbian 流程成功编译的工作树一致。

## 目标设备

目标设备来自 Armbian 的 `nas-lite.csc` 配置，不是本项目使用的编译主机：

| 项目 | 目标配置 |
| --- | --- |
| 目标名称 | `nas-lite` |
| SoC | Rockchip RK3568 |
| 内存配置 | 4 GB RAM |
| 存储/接口描述 | NVMe、eMMC、USB 2.0、USB 3.0、SATA |
| U-Boot 配置 | `rk3568-nas-lite_defconfig` |
| 默认设备树 | `rockchip/rk3568-nas-lite.dtb` |
| 镜像分区表 | GPT |
| 启动场景 | SPL + Rockchip 固件 |

这里的目标硬件信息来自板级构建配置。当前没有在目标 NAS Lite 实物上完成启动、存储和接口逐项验证，因此不能把编译主机的硬件信息当作目标板信息。

## 编译环境

本项目使用一台独立设备进行编译。该设备仅作为编译主机，不是 U-Boot 的目标设备：

| 项目 | 编译主机实测信息 |
| --- | --- |
| 设备树型号 | `OEC BOX WXY4` |
| 架构 | AArch64 |
| CPU | 4 x ARM Cortex-A55 |
| CPU 频率 | 408 MHz - 1800 MHz |
| 内存 | 约 4 GB（Linux 可用 3.8 GiB） |
| 系统 | Armbian，Linux `6.1.141-rk35xx-ophub` |
| 编译器 | `aarch64-linux-gnu-gcc` |

编译主机上的 SATA 和 eMMC 仅用于存放源码、构建目录及其他系统数据，与目标 NAS Lite 的存储配置无关。

## 源码与板级支持

- U-Boot 基线提交：`752ac3f2fdcfe9427ca8868d95025aacd48fc00b`
- 默认配置：`configs/rk3568-nas-lite_defconfig`
- 设备树：`arch/arm/dts/rk3568-nas-lite.dts`
- 公共 U-Boot 设备树修改：`arch/arm/dts/rk3568-u-boot.dtsi`
- 默认设备树：`rk3568-nas-lite`
- Rockchip SoC：`RK3568`

仓库中保留了后续 DG Linux 设备树适配所需的依赖文件，但默认
`rk3568-nas-lite.dts` 使用本次实际编译通过的 Armbian NAS Lite 版本。

## 编译

在 Linux/AArch64 交叉编译环境中执行：

```bash
make rk3568-nas-lite_defconfig
make KCFLAGS=-Wno-error -j$(nproc)
```

`KCFLAGS=-Wno-error` 用于兼容较新的 GCC。该 U-Boot 版本中的部分旧代码会触发
新 GCC 的警告升级错误，例如 `common/edid.c`、`common/command.c` 和
`drivers/mmc/rockchip_dw_mmc.c`。

### Armbian 流程

本次生成 `idbloader.img` 使用的是 `alerice/armbian-build` 的 `nas-lite` 板级流程。
在 Armbian Build 仓库中安装本项目附带的 GCC 13 兼容扩展：

```bash
mkdir -p userpatches/extensions
cp /path/to/u-boot-nas-lite/contrib/armbian/nas-lite-gcc13-compat.sh \
  userpatches/extensions/

ALLOW_ROOT=yes COLUMNS=160 ENABLE_EXTENSIONS=nas-lite-gcc13-compat \
  DEBIAN_FRONTEND=noninteractive \
  ./compile.sh uboot BOARD=nas-lite BRANCH=vendor ARTIFACT_IGNORE_CACHE=yes
```

`ALLOW_ROOT=yes` 只在使用 root 构建时需要。普通用户运行时可以省略。

## 已验证的编译产物

当前配置已在编译主机上完成 Armbian U-Boot 编译和打包。编译成功只说明源码和配置可以生成镜像，不代表已经在目标 NAS Lite 实物上完成启动验证。

```text
idbloader.img    296960 bytes
u-boot.itb      1548288 bytes
```

SHA-256：

```text
e0fe8ab7d4a0c6dcd304a2a3bac46a0924b893cd1a836aa2410e8fe7291a49bb  idbloader.img
6de50ef4b5f8d3aa27e2b9d4ae7ce1bbe38f1ba2e26ae84f93065c114e4e77e2  u-boot.itb
```

Armbian 软件包名称：

```text
linux-u-boot-nas-lite-vendor_24.11.0-trunk_arm64__2017.09-S752a-P8d10-Hd763-V46d3-Bda0a-R448a.deb
```

GitHub 仓库只提交源码和构建说明，不提交本次生成的二进制文件。

编译配置确认如下：

```text
CONFIG_DEFAULT_DEVICE_TREE="rk3568-nas-lite"
```

## 固件依赖

Armbian 板级配置中使用的 DDR 和 BL31 固件名称为：

```text
rk35/rk3568_ddr_1056MHz_v1.13.bin
rk35/rk3568_bl31_v1.33.elf
```

本项目仅包含 U-Boot 源码和板级支持，不包含上述二进制固件。刷写前请确认 DDR、BL31、启动介质和分区布局与实际目标板匹配。

## 相关项目

- [Armbian NAS Lite 配置](https://github.com/alerice/armbian-build/blob/main/config/boards/nas-lite.csc)
- [Orange Pi U-Boot 基线](https://github.com/orangepi-xunlong/u-boot-orangepi)
